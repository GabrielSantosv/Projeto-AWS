spring:
  datasource:
    url: jdbc:h2:mem:fiscal_db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false

aws:
  endpoint: http://localhost:4566
  region: us-east-1
  sns:
    saga-topic-arn: arn:aws:sns:us-east-1:000000000000:saga-events-topic
  sqs:
    fiscal-queue-url: http://localhost:4566/000000000000/fiscal-service-queue
    poll-interval-ms: 2000
    polling-enabled: false

outbox:
  poll-interval-ms: 100
  batch-size: 10
  polling-enabled: false
